# Database Configuration
# The absolute path to the SQLite database file
# If not set, defaults to: {electron-app-dir}/invoicegen.db
DATABASE_URL=/Users/harshjajal/Documents/invoice generator/electron-app/invoicegen.db

# Note: better-sqlite3 uses file paths directly, not connection strings
# The application will use process.env.DATABASE_URL if set, otherwise falls back to default location
